# jshark

The authoritative README for this repository lives at the repo root:
see `README.md` there (monorepo: `packages/`, `examples/`, `wasm/`).

This copy exists only so `cabal sdist` ships a README; release tooling
is expected to sync it from the root before publishing.
