'use strict';

/**
 * Web Worker tile loop: SharedArrayBuffer grids + Atomics generation barrier.
 * Scalar LUT stepping only (no wasm on worker thread).
 */
(function(global){function computeNextByte(top,cur,bot,lt,lc,lb,rt,rc,rb){let out=0;for(let bit=0;bit<8;bit++){const alive=(cur>>bit)&1;const left=bit>0?(cur>>(bit-1))&1:lc;const right=bit<7?(cur>>(bit+1))&1:rc;const topL=bit>0?(top>>(bit-1))&1:lt;const topC=(top>>bit)&1;const topR=bit<7?(top>>(bit+1))&1:rt;const botL=bit>0?(bot>>(bit-1))&1:lb;const botC=(bot>>bit)&1;const botR=bit<7?(bot>>(bit+1))&1:rb;const n=topL+topC+topR+left+right+botL+botC+botR;const next=alive?n===2||n===3:n===3;if(next)out|=1<<bit;}return out;}function createLifeLUT(){const LUT=new Uint8Array(65536);for(let key=0;key<65536;key++){const top=(key>>8)&255,cur=key&255;let out=0;for(let bit=0;bit<8;bit++){const alive=(cur>>bit)&1;const left=bit>0?(cur>>(bit-1))&1:0;const right=bit<7?(cur>>(bit+1))&1:0;const topL=bit>0?(top>>(bit-1))&1:0;const topC=(top>>bit)&1;const topR=bit<7?(top>>(bit+1))&1:0;const n=topL+topC+topR+left+right;const next=alive?n===2||n===3:n===3;if(next)out|=1<<bit;}LUT[key]=out;}return LUT;}function stepChunk(LUT,top,cur,bot,lt,lc,lb,rt,rc,rb){const edge=lt|lc|lb|rt|rc|rb;if((top|cur|bot|edge)===0)return 0;if(bot===0&&edge===0)return LUT[(top<<8)|cur];return computeNextByte(top,cur,bot,lt,lc,lb,rt,rc,rb);}function stepRegionLUT(LUT,gridA,gridB,w,h,y0,y1){const yStart=Math.max(1,y0);const yStop=Math.min(h-1,y1);for(let y=yStart;y<yStop;y++){const topOff=(y-1)*w,curOff=y*w,botOff=(y+1)*w;gridB.fill(0,curOff,curOff+w);const bytes=((w+7)/8)|0;for(let xb=0;xb<bytes;xb++){const x0=xb*8;if(x0>=w)continue;const leftCol=x0-1,rightCol=x0+8;const lt=x0>0&&(gridA[topOff+leftCol]&1)?1:0;const lc=x0>0&&(gridA[curOff+leftCol]&1)?1:0;const lb=x0>0&&(gridA[botOff+leftCol]&1)?1:0;const rt=rightCol<w&&(gridA[topOff+rightCol]&1)?1:0;const rc=rightCol<w&&(gridA[curOff+rightCol]&1)?1:0;const rb=rightCol<w&&(gridA[botOff+rightCol]&1)?1:0;let top=0,cur=0,bot=0;for(let b=0;b<8;b++){const x=x0+b;if(x>=w)continue;const sh=1<<b;if(gridA[topOff+x]&1)top|=sh;if(gridA[curOff+x]&1)cur|=sh;if(gridA[botOff+x]&1)bot|=sh;}if((top|cur|bot|lt|lc|lb|rt|rc|rb)===0)continue;const next=stepChunk(LUT,top,cur,bot,lt,lc,lb,rt,rc,rb);for(let b=0;b<8;b++){const x=x0+b;if(x>=w)continue;gridB[curOff+x]=next&(1<<b)?1:0;}}}if(y0===0)gridB.set(gridA.subarray(0,w));if(y1>=h){const botOff=(h-1)*w;gridB.set(gridA.subarray(botOff,h*w),botOff);}}global.LifeLUT={createLifeLUT,stepRegionLUT};})(typeof self!=='undefined'?self:globalThis);

(function () {const LifeLUT=self.LifeLUT;let LUT=null;let gridA=null;let gridB=null;let sync=null;let y0=0;let y1=0;let w=0;let h=0;function stepTile(){if(!LUT)LUT=LifeLUT.createLifeLUT();LifeLUT.stepRegionLUT(LUT,gridA,gridB,w,h,y0,y1);}self.onmessage=function(ev){const msg=ev.data;if(msg.type!=='init')return;sync=new Int32Array(msg.sab,0,8);const headerBytes=32;const gridBytes=msg.w*msg.h;gridA=new Uint8Array(msg.sab,headerBytes,gridBytes);gridB=new Uint8Array(msg.sab,headerBytes+gridBytes,gridBytes);y0=msg.y0;y1=msg.y1;w=msg.w;h=msg.h;LUT=LifeLUT.createLifeLUT();self.postMessage({type:'ready',workerId:msg.workerId});let lastGen=Atomics.load(sync,0);for(;;){Atomics.wait(sync,0,lastGen);lastGen=Atomics.load(sync,0);if(Atomics.load(sync,1)!==1)continue;stepTile();const finished=Atomics.add(sync,2,1)+1;const total=Atomics.load(sync,3);if(finished===total){Atomics.store(sync,1,2);Atomics.notify(sync,1,1);}}};})();